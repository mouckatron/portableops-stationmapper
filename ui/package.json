{
  "name": "stationmapper-ui",
  "version": "1.0.0",
  "description": "UI for stationmapper API",
  "scripts": {
    "start": "NODE_ENV=development parcel serve index.html --dist-dir run",
    "build": "NODE_ENV=production parcel build --public-url . --no-source-maps index.html"
  },
  "dependencies": {
    "ol": "latest"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  },
  "browserslist": "defaults"
}
